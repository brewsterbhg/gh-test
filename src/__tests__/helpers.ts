// import { mapLabelsToUsers, normalizeLabels, reduceUsers, formatSlackBlocks } from '../helpers';

// describe("helpers", () => {
//     describe("mapLabelsToUsers", () => {

//     })

//     describe("normalizeLabels", () => {
//         it("splits a comma-delimited string into an array", () => {
//             const expected = ['foo', 'bar', 'baz']
//             const actual = normalizeLabels("foo, bar, baz");

//             expect(actual).toEqual(expected);
//         })

//         it("returns an empty array if no args are provided", () => {
//             const expected: string[] = [];
//             const actual = mapLabelsToUsers("")

//             expect(actual).toEqual(expected);
//         })
//     })

//     describe("normalizeLabels", () => {

//     })

//     describe("reduceUsers", () => {

//     })
// })
// export const mapLabelsToUsers = (req: RequestContext): string[] => {

// export const normalizeLabels = (labels: string): string[] => {

// export const reduceUsers = (acc: string[], cur: string): string[] => {

// export const formatSlackBlocks = (
